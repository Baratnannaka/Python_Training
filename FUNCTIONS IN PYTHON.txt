FUNCTIONS IN PYTHON
  def function_name

  1)declare & defining a function:
    def functionname(optional argument)
        suit
2)default value to arguments:
    def sayhello(name='bharath'):
      print(name);
3)lambda functions in python: anonymous
    lambda arg1,arg2:arg1+arg2
      
      f = lambda a,b : a+bharath
      print (f(10,20))
